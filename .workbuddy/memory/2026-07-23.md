# 2026-07-23

## 云端部署就绪化（v3.3）
- 用户要求「部署到云端」，让团队（含异地）能访问带后端的考核平台。
- 重要约束：WorkBuddy 内置 cloudstudio-deploy 仅支持静态站点，跑不了 Node 后端；而本平台是「前端 + Node 后端」一体服务，纯静态托管会导致 API 全部失效、数据各自存浏览器互不相通。因此一键静态部署不可用。
- 已做的云就绪改造：
  - `server.js` 监听改为绑定 `0.0.0.0`（云平台容器外部可访问）。
  - 新增可用环境变量 `DB_PATH`（默认 `./data/db.json`），云部署挂持久盘时设为盘内路径防数据丢失。
  - 新增 `package.json`（start: `node server.js`，engines node>=18，零外部依赖）。
  - 新增 `.gitignore`（忽略 node_modules/data/）。
  - 新增 `README.md`：含 Render 免费部署步骤（连 GitHub → Web Service → start `node server.js` → 挂 Persistent Disk `/data` 并设 `DB_PATH=/data/db.json`）、Railway/Fly 备选、权限与数据说明。
- 冒烟测试（PORT=3210, DB_PATH=测试文件）：GET / 200、公开 /api/config 200、免登录建工单 200、免登录审核 403、管理员审核 200、DB_PATH 落盘生效，全部通过。
- 仍未完成的真实上线：需用户用自己的免费云账号（Render/Railway）连接仓库完成部署一步；本环境无外部账号无法代为推送。已向用户说明并给出精确步骤；另提供「本机+公网隧道」即时方案作为免注册的备选。

## 工单明细增加管理员删除（v3.4）
- 用户要求：NGR 错误升级工单明细里，加一个管理员可删除提交记录的功能。
- 前端改动（index.html）：
  - `renderPeriodTable` 的表头与每行末尾，仅当 `isAdmin()` 时追加「操作(管理员)」列，含「删除」按钮，调用 `App.delOrder(id)`。
  - 新增 `delOrder(id)`：confirm 后 DELETE `/api/orders/:id`，成功后重新 `openPeriod` 刷新明细、`renderPeriodGrid` 更新周期卡片数、`loadCounts` 更新模块计数；并暴露到 App 返回对象。
- 后端：`/api/orders/:id` DELETE 早已存在且服务端强制 `role==='admin'`（非管理员 403），无需改动。
- 验证：管理员删除 200；免登录删除 403（需要管理员权限）；JS 语法校验通过。

## 公网访问：localtunnel 隧道（v3.5，即时上线）
- 用户要求：网站上云端，让其他人能点击进入。
- 采用「本机 + 公网隧道」即时方案（真·云 Node 托管需用户自有账号，之前已说明）。
- 工具：localtunnel，装在隔离环境 `C:\Users\belanghuang\.workbuddy\binaries\node\workspace`。
- 启动命令：`cd <workspace> && npx localtunnel --port 3000`（本机后端须在 3000 运行）。
- 本次公网网址：https://salty-insects-punch.loca.lt （每次重启会变；可加 `--subdomain ngr-kaohe` 尝试固定）。
- 首次访问 localtunnel 会有拦截页，需点击继续；若要求密码则输入本机公网IP（本次为 43.132.141.20，用 `curl https://loca.lt/mytunnelpassword` 查询）。
- 限制：用户电脑须常开且服务须运行；隧道进程或机器关闭则链接失效。数据仍存本机 data/db.json。
- 已验证：外部经隧道访问 /api/config 返回 200。
